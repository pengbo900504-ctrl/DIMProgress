```yaml
name: Build iOS Tweak
on: workflow_dispatch:
jobs: build: runs-on: macos-latest steps: - uses: actions/checkout@v4 - run: brew install theos - run: | cd DIMProgress-Source export THEOS=/opt/theos make clean && make && make package - uses: actions/upload-artifact@v4 with: name: DIMProgress-deb path: DIMProgress-Source/packages/*.deb
```
